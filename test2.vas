message: db "Hello world!\n"

return:
    mov rax, 60
    mov rdi, 0
    syscall

_start:
    mov rax, 1
    mov rdi, 0
    add rdi, 1
    mov rsi, message
    mov rdx, 13
    syscall

    call return